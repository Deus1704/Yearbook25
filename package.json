{
  "name": "yearbook25",
  "version": "1.0.0",
  "description": "Yearbook application for IIT Gandhinagar",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node server.js",
    "dev": "cd backend && nodemon server.js",
    "prepare-render": "cd backend && node scripts/prepare-render-deployment.js"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
