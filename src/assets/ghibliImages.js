// Empty array to satisfy webpack imports
const ghibliImages = [];

// Helper function that returns null since we don't have any images
export const getRandomGhibliImage = () => {
  return null;
};

export default ghibliImages;
